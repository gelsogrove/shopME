/**
 * Default workspace ID used when no specific workspace is provided
 */
export const DEFAULT_WORKSPACE_ID = "cm9hjgq9v00014qk8fsdy4ujv";

/**
 * Returns the default workspace ID to use when none is explicitly provided
 */
export function getDefaultWorkspaceId(): string {
  return DEFAULT_WORKSPACE_ID;
} 